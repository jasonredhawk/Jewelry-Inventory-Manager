<Window x:Class="Moonglow_DB.Views.StatusUpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Transfer Status" 
        Width="500" 
        Height="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="450"
        MinWidth="450"
        Background="#F5F5F5">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FF9800" Padding="15" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🔄" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="Update Transfer Status" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Foreground="White" 
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Current Status -->
        <Border Grid.Row="1" Background="#E3F2FD" Padding="15" Margin="15,0,15,10" CornerRadius="8">
            <TextBlock x:Name="txtCurrentStatus" 
                       Text="Current Status: Created" 
                       FontWeight="Bold" 
                       FontSize="14"/>
        </Border>

        <!-- New Status Label -->
        <TextBlock Grid.Row="2" 
                   Text="Select New Status:" 
                   FontWeight="SemiBold" 
                   FontSize="14" 
                   Margin="15,15,15,5"/>

        <!-- Status ComboBox -->
        <ComboBox x:Name="cmbStatus" 
                  Grid.Row="3" 
                  Margin="15,5,15,15" 
                  Padding="10,8" 
                  FontSize="12"
                  Style="{StaticResource ModernComboBoxStyle}"/>

        <!-- Help Text - Now in scrollable area -->
        <ScrollViewer Grid.Row="4" 
                      VerticalScrollBarVisibility="Auto" 
                      Margin="15,0,15,15">
            <Border Background="#FFF8E1" 
                    Padding="15" 
                    CornerRadius="8" 
                    BorderBrush="#FFC107" 
                    BorderThickness="1">
                <TextBlock x:Name="txtHelp" 
                           Text="Select the new status for this transfer. You can skip intermediate states if needed." 
                           TextWrapping="Wrap" 
                           FontSize="12" 
                           LineHeight="18"/>
            </Border>
        </ScrollViewer>

        <!-- Button Panel - Fixed at bottom -->
        <Border Grid.Row="5" 
                Background="White" 
                Padding="15" 
                BorderBrush="#E0E0E0" 
                BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <Button x:Name="btnUpdate" 
                        Content="✅ Update Status" 
                        Background="#4CAF50"
                        Foreground="White"
                        Padding="15,8"
                        Click="btnUpdate_Click"/>
                <Button x:Name="btnCancel" 
                        Content="❌ Cancel" 
                        Background="#F44336"
                        Foreground="White"
                        Padding="15,8"
                        Click="btnCancel_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window> 