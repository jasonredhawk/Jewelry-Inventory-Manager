<Window x:Class="Moonglow_DB.Views.EditOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Order" Height="400" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#009688" Padding="15">
            <TextBlock Text="Edit Order" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       Foreground="White" 
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Content -->
        <StackPanel Grid.Row="1" Margin="20" VerticalAlignment="Center">
            <TextBlock Text="Edit Order functionality will be implemented soon." 
                       FontSize="16" 
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,20"/>
            
            <TextBlock Text="This will include:" 
                       FontSize="14" 
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,10"/>
            
            <StackPanel Margin="20">
                <TextBlock Text="• Modify order details" Margin="0,5"/>
                <TextBlock Text="• Update order status" Margin="0,5"/>
                <TextBlock Text="• Add/remove products" Margin="0,5"/>
                <TextBlock Text="• Recalculate totals" Margin="0,5"/>
            </StackPanel>
        </StackPanel>

        <!-- Buttons -->
        <Border Grid.Row="2" Background="White" Padding="15" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnCancel" 
                        Content="Close" 
                        Style="{StaticResource DangerButtonStyle}"
                        Click="btnCancel_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window> 