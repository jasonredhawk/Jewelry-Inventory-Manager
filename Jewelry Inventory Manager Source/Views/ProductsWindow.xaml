<Window x:Class="Moonglow_DB.Views.ProductsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Products Management" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📦" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="Products Management" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Foreground="White" 
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Toolbar -->
        <Border Grid.Row="1" Background="White" Padding="15" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="btnAddProduct" 
                        Content="➕ Add Product" 
                        Style="{StaticResource SuccessButtonStyle}"
                        Click="btnAddProduct_Click"/>
                
                <Button x:Name="btnEditProduct" 
                        Content="✏️ Edit" 
                        Style="{StaticResource ModernButtonStyle}"
                        Click="btnEditProduct_Click"/>
                
                <Button x:Name="btnDeleteProduct" 
                        Content="🗑️ Delete" 
                        Style="{StaticResource DangerButtonStyle}"
                        Click="btnDeleteProduct_Click"/>
                
                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="20,0"/>
                
                <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="txtSearch" 
                         Width="200" 
                         Style="{StaticResource ModernTextBoxStyle}"
                         VerticalAlignment="Center"
                         TextChanged="txtSearch_TextChanged"/>
            </StackPanel>
        </Border>

        <!-- DataGrid -->
        <DataGrid x:Name="dgProducts" 
                  Grid.Row="2" 
                  Margin="15"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  Background="White"
                  BorderBrush="#E0E0E0"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  SelectionChanged="dgProducts_SelectionChanged">
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="120"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category.Name}" Width="150"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="200"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C}" Width="100"/>
                <DataGridTextColumn Header="Component Cost" Binding="{Binding ComponentCost, StringFormat=C}" Width="120"/>
                <DataGridTextColumn Header="Current Stock" Binding="{Binding CurrentStock}" Width="100"/>
                <DataGridTextColumn Header="Min Stock" Binding="{Binding MinimumStock}" Width="100"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="80"/>
                <DataGridTextColumn Header="Last Modified" Binding="{Binding LastModified, StringFormat=MM/dd/yyyy}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window> 