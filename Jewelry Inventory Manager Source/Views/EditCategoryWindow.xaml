<Window x:Class="Moonglow_DB.Views.EditCategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Category" Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#9C27B0" Padding="15">
            <TextBlock Text="Edit Category" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       Foreground="White" 
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <!-- Category Name -->
                <TextBlock Text="Category Name *" 
                           FontWeight="Bold" 
                           Margin="0,0,0,5"/>
                <TextBox x:Name="txtName" 
                         Style="{StaticResource ModernTextBoxStyle}"
                         Margin="0,0,0,15"
                         TextChanged="txtName_TextChanged"/>

                <!-- Description -->
                <TextBlock Text="Description" 
                           FontWeight="Bold" 
                           Margin="0,0,0,5"/>
                <TextBox x:Name="txtDescription" 
                         Style="{StaticResource ModernTextBoxStyle}"
                         Height="80"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         Margin="0,0,0,15"
                         TextChanged="txtDescription_TextChanged"/>

                <!-- Parent Category -->
                <TextBlock Text="Parent Category (Optional)" 
                           FontWeight="Bold" 
                           Margin="0,0,0,5"/>
                <ComboBox x:Name="cmbParentCategory" 
                          Style="{StaticResource ModernComboBoxStyle}"
                          DisplayMemberPath="Name"
                          SelectedValuePath="Id"
                          Margin="0,0,0,15"
                          SelectionChanged="cmbParentCategory_SelectionChanged"/>

                <!-- Sort Order -->
                <TextBlock Text="Sort Order" 
                           FontWeight="Bold" 
                           Margin="0,0,0,5"/>
                <TextBox x:Name="txtSortOrder" 
                         Style="{StaticResource ModernTextBoxStyle}"
                         PreviewTextInput="txtSortOrder_PreviewTextInput"
                         Margin="0,0,0,15"
                         TextChanged="txtSortOrder_TextChanged"/>

                <!-- Active Status -->
                <CheckBox x:Name="chkIsActive" 
                          Content="Active Category" 
                          Margin="0,0,0,15"
                          Checked="chkIsActive_Changed"
                          Unchecked="chkIsActive_Changed"/>

                <!-- Summary -->
                <Border Background="#F0F0F0" 
                        CornerRadius="5" 
                        Padding="15" 
                        Margin="0,10">
                    <StackPanel>
                        <TextBlock Text="Category Summary" 
                                   FontWeight="Bold" 
                                   Margin="0,0,0,10"/>
                        <TextBlock x:Name="txtSummary" 
                                   Text="Please fill in the category details above."
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <Border Grid.Row="2" Background="White" Padding="15" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnSave" 
                        Content="Save Changes" 
                        Style="{StaticResource SuccessButtonStyle}"
                        Click="btnSave_Click"
                        Margin="0,0,10,0"/>
                <Button x:Name="btnCancel" 
                        Content="Cancel" 
                        Style="{StaticResource DangerButtonStyle}"
                        Click="btnCancel_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window> 