<Window x:Class="Moonglow_DB.Views.EditProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Product"
        Height="600" Width="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../Styles/SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="15,10" Margin="0,0,0,20">
            <TextBlock Text="Edit Product" 
                       Foreground="White" 
                       FontSize="18" 
                       FontWeight="Bold"/>
        </Border>
        
        <!-- Form -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- SKU -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="SKU:" VerticalAlignment="Center" Margin="0,5"/>
                    <TextBox x:Name="txtSKU" Grid.Row="0" Grid.Column="1" 
                             Style="{StaticResource ModernTextBoxStyle}" 
                             IsReadOnly="True"/>
                    
                    <!-- Name -->
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Name:" VerticalAlignment="Center" Margin="0,5"/>
                    <TextBox x:Name="txtName" Grid.Row="1" Grid.Column="1" 
                             Style="{StaticResource ModernTextBoxStyle}"/>
                    
                    <!-- Description -->
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Description:" VerticalAlignment="Center" Margin="0,5"/>
                    <TextBox x:Name="txtDescription" Grid.Row="2" Grid.Column="1" 
                             Style="{StaticResource ModernTextBoxStyle}" 
                             Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
                    
                    <!-- Category -->
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Category:" VerticalAlignment="Center" Margin="0,5"/>
                    <ComboBox x:Name="cmbCategory" Grid.Row="3" Grid.Column="1" 
                              Style="{StaticResource ModernComboBoxStyle}" 
                              DisplayMemberPath="Name" SelectedValuePath="Id"/>
                    
                    <!-- Price -->
                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Price:" VerticalAlignment="Center" Margin="0,5"/>
                    <TextBox x:Name="txtPrice" Grid.Row="4" Grid.Column="1" 
                             Style="{StaticResource ModernTextBoxStyle}"/>
                    
                    <!-- Is Active -->
                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Active:" VerticalAlignment="Center" Margin="0,5"/>
                    <CheckBox x:Name="chkIsActive" Grid.Row="5" Grid.Column="1" 
                              VerticalAlignment="Center" Margin="0,5"/>
                </Grid>
                
                <!-- Components Section -->
                <Border Background="#F5F5F5" CornerRadius="5" Padding="15" Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="Product Components" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                        
                        <!-- Add Component Section -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <ComboBox x:Name="cmbComponent" Grid.Column="0" 
                                      Style="{StaticResource ModernComboBoxStyle}" 
                                      DisplayMemberPath="Name" SelectedValuePath="Id"
                                      Margin="0,0,10,0"/>
                            <TextBox x:Name="txtComponentQuantity" Grid.Column="1" 
                                     Style="{StaticResource ModernTextBoxStyle}" 
                                     Text="1" Margin="0,0,10,0"/>
                            <Button x:Name="btnAddComponent" Grid.Column="2" 
                                    Content="Add Component" 
                                    Style="{StaticResource ModernButtonStyle}"
                                    Click="btnAddComponent_Click"/>
                        </Grid>
                        
                        <!-- Components DataGrid -->
                        <DataGrid x:Name="dgComponents" 
                                  AutoGenerateColumns="False" 
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  IsReadOnly="True"
                                  Height="150">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Component" Binding="{Binding ComponentName}" Width="*"/>
                                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="80"/>
                                <DataGridTemplateColumn Header="Actions" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Remove" 
                                                    Style="{StaticResource DangerButtonStyle}"
                                                    Click="btnRemoveComponent_Click"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="btnSave" Content="Save Changes" 
                    Style="{StaticResource SuccessButtonStyle}" 
                    Click="btnSave_Click"/>
            <Button x:Name="btnCancel" Content="Cancel" 
                    Style="{StaticResource DangerButtonStyle}" 
                    Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window> 